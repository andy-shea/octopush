import styled from 'styled-components';

const ServerIcon = styled.span`
  display: block;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 1px solid;
  cursor: pointer;
`;

export default ServerIcon;
